package infoblox

import (
	. "github.com/onsi/ginkgo/v2"
	// . "github.com/onsi/gomega"
)

var _ = Describe("Infoblox Client", func() {
	// We don't need to test basic client creation, since it's already tested in BeforeSuite.

	// hostCfg, authCfg, _ := InfobloxConfigFromEnv()
	//networkView := getInfobloxTestEnvVar("network_view", "default")

	Describe("creating a new client", func() {
		// It("can parse a Kubernetes secret to get the necessary credentials", func() {

		// })

	})
})
